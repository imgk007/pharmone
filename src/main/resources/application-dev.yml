#database properties
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pharmone
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

resilience4j:
  timelimiter:
    instances:
      medicineService:
        timeoutDuration: 10s # Relaxed for local work